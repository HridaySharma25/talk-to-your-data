# Talk-to-Your-Data — Python dependencies
# Python 3.11+ required. Pinned to minimum tested versions; the resolver is free
# to pick newer compatible releases. Install with: pip install -r requirements.txt

# --- Data handling + database layer ---
pandas>=2.2.0
SQLAlchemy>=2.0.30          # ORM/Core engine over SQLite

# --- LLM: Google Gemini (direct SDK, no framework wrapper) ---
google-generativeai>=0.8.3  # gemini-2.5-flash for NL->SQL and result summaries

# --- Web UI + visualization ---
streamlit>=1.40.0
plotly>=5.24.0              # interactive charts (project standard, not matplotlib)

# --- SQL parsing / safety validation ---
sqlparse>=0.5.0

# --- Resilience: retry with exponential backoff on API calls ---
tenacity>=9.0.0

# --- Config / secrets management ---
python-dotenv>=1.0.1

# --- Testing + evaluation harness ---
pytest>=8.3.0
